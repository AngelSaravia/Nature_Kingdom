name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - Angel's-branch
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - Angel's-branch

permissions:
  contents: write

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false

      # Fix permissions and clean Git configuration
      - name: Fix Git configuration permissions
        run: |
          sudo chown -R $(whoami) .git/
          sudo chmod -R u+rw .git/
          git config --global --unset-all http.https://github.com/.extraheader || true
          git config --local --unset-all http.https://github.com/.extraheader || true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18.x"

      - name: Install Dependencies
        run: npm install

      # List directory structure to help with debugging
      - name: List directory structure
        run: |
          echo "Root directory contents:"
          ls -la
          echo "Checking if specific directories exist:"
          find . -type d -maxdepth 2 | sort

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ZEALOUS_TREE_05AFCCC10 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          ###### Repository/Build Configurations ######
          app_location: "/client" # App source code path
          api_location: "api" # API source code path - optional
          output_location: "/" # For static sites with no build process, use the root directory
          skip_app_build: true # Skip the build process since your app doesn't need a build step
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ZEALOUS_TREE_05AFCCC10 }}
          action: "close"
